# 長期マクロ履歴 GitHub Pages 仕様

grill-me で確定した共通理解。実装はこの仕様に従う。

## 1. 目的

歴史を学ぶための静的サイト。S&P500、日経平均、ドル円、米10年国債利回りの長期チャートと、急落・急変時期の事件タイトル年表を示す。

- 投資助言、売買シグナル、ポートフォリオ機能は持たない
- ライブ価格は出さない
- 免責をフッターに常時表示する

## 2. 対象系列

| ID | 表示名 | 単位の扱い | 「急落」の意味 |
|----|--------|------------|----------------|
| `sp500` | S&P500 | 指数 | ピークからのドローダウン |
| `nikkei` | 日経平均 | 指数 | ピークからのドローダウン |
| `usdjpy` | ドル円 | 円/ドル | 短期間の急騰・急落（両方向） |
| `us10y` | 米10年国債利回り | ％ | 短期間の急上昇・急低下（両方向） |

## 3. 期間

- 「100年」は目標であり、系列ごとに取得できる最長を使う
- 足りない期間は空にする。補間や別指数によるつなぎはしない
- 各チャートとフッターに、その系列の開始日・終了日を出す
- ページ上の表現例: 「長期履歴（系列ごとに取得可能な最長期間）」

## 4. データ

### 4.1 配信

- 閲覧時に外部 API は呼ばない
- 取得スクリプトを一度実行し、結果をリポジトリにコミットする
- 定期自動更新はしない
- フッターにデータ取得日（スナップショット日）を出す

### 4.2 配置

```
docs/data/sp500.json
docs/data/nikkei.json
docs/data/usdjpy.json
docs/data/us10y.json
docs/data/meta.json      # 取得日、出典、各系列の頻度・開始終了
docs/data/events.json
```

系列 JSON の形:

```json
{
  "id": "sp500",
  "name": "S&P500",
  "frequency": "daily | monthly",
  "source": "出典名",
  "sourceUrl": "https://...",
  "points": [{ "date": "YYYY-MM-DD", "value": 0 }]
}
```

日付昇順。欠損日は点を置かない。

### 4.3 ソース（推奨。スクリプトに URL を残す）

実装時にライセンスと実在する最長系列を確認して選ぶ。候補:

- 米10年: FRED `DGS10`（不足があれば長期金利の公開系列）
- ドル円: FRED `DEXJPUS`
- 日経: FRED `NIKKEI225` または同等の公開系列
- S&P500: FRED 日次が短い場合は Shiller 等の長期月次

日次があれば日次、なければ月次。系列間で頻度が違ってよい。チャートに頻度を表示する。

出典名・ライセンス・再配布条件を README とフッターに書く。

## 5. 急落・急変の検出

定数としてコードに置く。初期値:

| 系列 | 定義 |
|------|------|
| S&P / 日経 | 直近ピークからの下落が **10% 以上** の区間 |
| ドル円 | **60 営業日**（月次なら相当する窓）で **10% 超** の変化。円高・円安両方。同じ方向で日付が重なる検出は **1 区間に結合**する |
| 米10年 | 同じ窓で **100bp 超** の変化。上昇・低下両方。ドル円と同じく、同じ方向で重なる検出は結合する |

検出結果:

```json
{
  "series": "sp500",
  "start": "YYYY-MM-DD",
  "end": "YYYY-MM-DD",
  "kind": "drawdown | surge | drop",
  "magnitude": 0
}
```

- `events.json` の期間と重なれば事件タイトルを出す
- 重ならなければ「未注釈」と出す（検出は隠さない）

検出はビルド時でも閲覧時の JS でもよい。再現できること。推奨は閲覧時 JS（閾値変更が容易）。

## 6. 事件年表

人手のタイトルのみ。長文解説は置かない。

`docs/data/events.json`:

```json
[
  {
    "start": "2008-09-01",
    "end": "2009-03-31",
    "title": "リーマン・ショック",
    "series": []
  }
]
```

- `series` が空または省略: 全体に関連
- 入っている場合: その系列のチャート帯と年表フィルタに使う

初期タイトルの例（日付は実装時に出典付きで確定）:

- 世界恐慌
- ブラックマンデー
- アジア通貨危機
- ITバブル崩壊
- リーマン・ショック
- 欧州債務危機
- コロナショック
- ボルカー・ショック
- プラザ合意後の円高
- 急激な円安局面（2022年など）

## 7. UI

言語: 日本語のみ。指数名は上表の表示名。

1ページ構成（上から）:

1. タイトルと短い説明、免責の要約
2. チャート（独立4本）: S&P500 → 日経平均 → ドル円 → 米10年
3. 急落・急変区間を背景帯でハイライト
4. 共通のタイトル年表（時系列）。項目クリックで該当期間を強調
5. フッター: 出典、各系列の開始・終了、頻度、スナップショット日、免責全文

ズームは必須ではない。初期は全期間表示。簡易ズームがあってもよい。

4系列の正規化重ね合わせは初期対象外。

## 8. 技術

- GitHub Pages: リポジトリ設定で公開ディレクトリを `/docs`
- `docs/index.html`, `docs/css/`, `docs/js/`、データは `docs/data/`
- チャート: Chart.js（CDN）または同等の軽量ライブラリ
- 取得: `scripts/fetch_data.py`（Python）。初回実行して JSON をコミット。CI 定期実行はしない
- ルートに本 `spec.md` と、必要なら短い README（Pages の出し方と出典）

## 9. ディレクトリ

```
spec.md
README.md
scripts/fetch_data.py
docs/index.html
docs/css/style.css
docs/js/app.js
docs/data/*.json
```

## 10. 対象外

- ライブ価格・API キー・閲覧時の外部取得
- ログイン、コメント、CMS
- 投資助言、推奨配分、シグナル
- 事件の本文解説
- 系列の人工的な 100 年つなぎ
- 初期の正規化オーバーレイ
- 月次自動更新

## 11. 受け入れ条件

- `/docs` を Pages に指定するとサイトが見える
- 4チャートが各系列の最長データで描画される
- 開始年が系列ごとに違い、それが表示される
- 株の 10% ドローダウン、為替・金利の急変が帯で見える
- 年表はタイトルのみ。帯と日付が重なると紐づく。重ならない検出は「未注釈」
- 外部 API なしで表示できる
- 日本語 UI と免責がある
